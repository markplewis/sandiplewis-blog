{
  "name": "sandi-plewis-blog",
  "version": "1.0.0",
  "scriptsComments": {
    "next-dev": "`next dev` starts the application in development mode with hot-code reloading, error reporting, and more",
    "next-build": "`next build` creates an optimized production build of the application",
    "next-start": "`next start` starts the application in production mode. The application should be compiled with `next build` first",
    "next-export": "`next export` allows you to export your app to static HTML, which can be run standalone without the need of a Node.js server",
    "sanity-dev": "`sanity start` start the development server for Sanity Studio (run this command within the studio folder)",
    "sanity-build": "`sanity build` builds the current Sanity configuration to a static bundle"
  },
  "scripts": {
    "next-dev": "next dev",
    "next-build": "next build",
    "next-start": "next start",
    "next-export": "next build && next export",
    "sanity-dev": "cp .env.local ./studio/.env.development && cd studio && sanity start",
    "sanity-build": "cd studio && sanity build ../public/studio -y && cd ..",
    "build": "npm run sanity-build && pwd && npm run next-build",
    "postinstall": "lerna bootstrap",
    "check": "npx npm-check -s -u"
  },
  "dependencies": {
    "@sanity/block-content-to-react": "^3.0.0",
    "@sanity/client": "^2.10.5",
    "@sanity/image-url": "^0.140.19",
    "classnames": "^2.2.6",
    "date-fns": "^2.22.1",
    "next": "^10.2.3",
    "next-sanity": "^0.2.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-hook-form": "^7.8.3",
    "swr": "^0.5.6"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^4.0.3",
    "@sanity/cli": "^2.10.5",
    "lerna": "^4.0.0",
    "postcss-preset-env": "^6.7.0",
    "tailwindcss": "^2.1.4"
  }
}
